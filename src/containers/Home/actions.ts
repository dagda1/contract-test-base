// import { ApiActionTypes, ApiAction } from '../../store/middleware';
import { HomeActionTypes, HomeClear } from './types';

export const clearState: HomeClear = {
  type: HomeActionTypes.HOME_CLEAR
};
